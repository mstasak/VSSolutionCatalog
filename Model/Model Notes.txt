Model Notes

1st Variation: EF
	Trying to use Entity Framework (for .Net Framework 4.8? or can I use Core?  I think 4.8),
	with a PostgreSQL database at localhost:5432.

	User and DB Setup:


	Schema will be designed in code classes, managed by EF.

	(CONNECT Postgres/Postgres)
	CREATE DATABASE VSSolCat
	  TEMPLATE DEFAULT;

	(CONNECT Postgres/VSSolCat)
	CREATE ROLE VSSolCatOwnerRole;
	CREATE SCHEMA VSSolCatSchema;

	GRANT ALL PRIVILEGES
	  ON ALL TABLES
	  IN SCHEMA VSSolCatSchema
	  TO VSSolCatOwnerRole
	  WITH GRANT OPTION;

	CREATE USER VSSolCat
	  WITH PASSWORD 'password';

	(CONNECT VSSolCat/Postgres)

	CREATE ROLE VSSolCatAdminRole;

	GRANT ALL PRIVILEGES
	  ON ALL TABLES
	  IN SCHEMA VSSolCatSchema
	  TO VSSolCatAdminRole;

	CREATE USER VSSolCatAdmin
	  WITH PASSWORD 'password'
	  IN ROLE VSSolCatAdminRole;

	GRANT CREATE ON SCHEMA VSSolCatSchema to VSSolCatAdminRole;


GRANT ALL PRIVILEGES
ON DATABASE VSSolCat
TO VSSolCatAdminRole

GRANT ALL PRIVILEGES
ON SCHEMA VSSolCatSchema
TO VSSolCatAdminRole



	CREATE ROLE VSSolCatUserRole;
	CREATE ROLE VSSolCatQueryRole;

	GRANT ALL PRIVILEGES /* change to SELECT,INSERT,UPDATE,DELETE,TRUNCATE, ??? */
	  ON ALL TABLES
	  IN SCHEMA VSSolCatSchema
	  TO VSSolCatUserRole;

needed?
GRANT ALL PRIVILEGES /* change to SELECT,INSERT,UPDATE,DELETE,TRUNCATE, ??? */
ON SCHEMA VSSolCatSchema
TO VSSolCatUserRole, VSSolCatUser
		
	GRANT SELECT
	  ON ALL TABLES
	  IN SCHEMA VSSolCatSchema
	  TO VSSolCatQueryRole;

	CREATE USER VSSolCatUser 
	  WITH PASSWORD 'password'
	  IN ROLE VSSolCatUserRole;

	CREATE USER VSSolCatQuery
	  WITH PASSWORD 'password'
	  IN ROLE VSSolCatQueryRole;

    (connect VSSolCat/VSSolCatAdmin)
	CREATE TABLE VSSolCatSchema.Solutions (
	    Id INT GENERATED BY DEFAULT AS IDENTITY,
        Name VARCHAR(80),
        Path VARCHAR(512),
        Size BIGINT,
        SlnCreated TIMESTAMP,
        SlnModified TIMESTAMP,
        Description TEXT,
        Language VARCHAR(80),
        Runtime VARCHAR(80),
        Rating INT,
        RelocateTo VARCHAR(512),
        Disposition TEXT,
        Created TIMESTAMP,
        Updated TIMESTAMP,
		UNIQUE(Name)
	);
ALTER USER vssolcatuser SET search_path TO vssolcatschema, public;
	DISCONNECT;

2nd Variation: Pure ADODB

Same DB Setup as with EF

3rd Variation: X#/XBase

Same DB Setup as with EF
